<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!-- Android 11+ visibility needed by the standalone manager. -->
    <queries>
        <package android:name="com.max.xiaoheihe" />
        <package android:name="top.nkbe.npatch" />
        <package android:name="af.shizuku.plus.api" />
        <package android:name="moe.shizuku.privileged.api" />
        <provider android:authorities="top.nkbe.npatch.remote" />
    </queries>

    <!--
      Modern libxposed API 102 module metadata lives in META-INF/xposed/:
        module.prop / java_init.list / scope.list.
      The launcher below is a manager UI only; hook execution still occurs in
      the target process through LSPosed or NPatch.
    -->
    <application
        android:name=".App"
        android:label="@string/app_name"
        android:description="@string/module_description"
        android:icon="@drawable/ic_betterheybox"
        android:roundIcon="@drawable/ic_betterheybox"
        tools:targetApi="25">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Required for Shizuku/Shizuku+ to deliver its Binder to this app. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <receiver
            android:name=".PreferenceReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.better.heybox.SET_BOOLEAN" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
